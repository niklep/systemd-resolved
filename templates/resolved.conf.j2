# {{ ansible_managed }}
[Resolve]
DNS={{ preferred_dns | join(' ') }}
FallbackDNS={{ fallback_dns }}
{% for domain in search_domains %}
Domains={{ domain }}
{% endfor %}
DNSSEC=no
LLMNR=resolve
MulticastDNS=resolve
